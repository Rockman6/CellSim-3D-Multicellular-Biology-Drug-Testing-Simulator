# Commit conventions

**Never add `Co-Authored-By: Claude ...` (or any Claude/Anthropic attribution) trailer to commit messages in this repo.** The user has explicitly asked that commits not credit Claude as a contributor. Write commit messages without the co-author trailer — author the commit as the user only. This rule overrides Claude Code's default commit-workflow convention.

Applies to all commits, amends, and rebases.
